# Implementation Plan: Call Records Dashboard

**Branch**: `002-call-records-dashboard` | **Date**: 2026-06-10 | **Spec**: [spec.md](./spec.md)

## Summary

Extend the existing Intoxalock POC with two new Lambda endpoints and a new frontend dashboard page. A `POST /api/v1/webhook/call-completed` endpoint receives ElevenLabs post-call webhooks and persists removal request records to a new DynamoDB table. A `GET /api/v1/removal-requests` endpoint returns all stored records. A new React page at `/requests` displays the records in a MUI table following the Intoxalock brand guidelines.

## Technical Context

**Language/Version**: TypeScript 5.x (frontend + backend) — same as feature 001

**Primary Dependencies**:
- Backend: Node.js 18 (Lambda `nodejs18.x`), `@aws-sdk/client-dynamodb`, `@aws-sdk/lib-dynamodb`, Zod
- Frontend: React 18, Vite 5, MUI v6, `react-router-dom` v6 (new dependency)

**Storage**: DynamoDB — new table `intoxalock-removal-requests`

**Testing**:
- Backend: Jest + `ts-jest`
- Frontend: Vitest + React Testing Library

**Target Platform**: AWS (Lambda + API Gateway v2 + DynamoDB + S3 + CloudFront)

**Project Type**: Extension of existing web application — new Lambda handlers + new frontend page

**Performance Goals**:
- Webhook persistence within 3 seconds (SC-001)
- Dashboard load within 3 seconds (SC-002)

**Constraints**:
- No authentication (POC)
- No pagination — full scan for POC volume
- No auto-refresh — manual page reload only
- Secrets via Lambda environment variables
- All copy in English

**Scale/Scope**: POC — low call volume, single region, no rate limiting

## Constitution Check

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Clean Architecture | ✅ PASS | New handlers are thin; logic delegated to use cases; DynamoDB access behind repository port |
| II. Spec-Driven | ✅ PASS | spec.md → clarify → plan workflow followed before any code |
| III. Test-First | ✅ PASS | Unit + integration tests defined in tasks before implementation tasks |
| IV. API-First | ✅ PASS | Both new endpoints contracted in `contracts/api.md` before handler code |
| V. Security & Compliance | ✅ PASS | No PII logged in plaintext; AWS credentials via env vars; unauthenticated endpoints are POC-scoped and documented |
| VI. Simplicity | ✅ PASS | Full scan (no GSI), no router abstraction, no shared layers — POC scope respected |

## Project Structure

### Documentation (this feature)

```text
specs/002-call-records-dashboard/
├── plan.md              ← this file
├── spec.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   └── api.md
├── checklists/
│   └── requirements.md
└── tasks.md             ← generated by /speckit-tasks
```

### Source Code additions

```text
src/backend/src/
├── domain/
│   ├── entities/
│   │   └── RemovalRequestRecord.ts          ← new domain entity
│   └── ports/
│       └── IRemovalRequestRepository.ts     ← new repository port
├── application/
│   └── useCases/
│       ├── RecordCallWebhookUseCase.ts       ← new: persist webhook data
│       └── GetRemovalRequestsUseCase.ts      ← new: return all records
├── infrastructure/
│   └── dynamo/
│       └── DynamoRemovalRequestRepository.ts ← new DynamoDB adapter
└── presentation/
    └── handlers/
        ├── webhookHandler.ts                 ← new Lambda handler (POST webhook)
        └── removalRequestsHandler.ts         ← new Lambda handler (GET records)

src/backend/tests/
├── unit/
│   ├── useCases/
│   │   ├── RecordCallWebhookUseCase.test.ts
│   │   └── GetRemovalRequestsUseCase.test.ts
│   └── handlers/
│       ├── webhookHandler.test.ts
│       └── removalRequestsHandler.test.ts
└── integration/
    └── dynamo/
        └── DynamoRemovalRequestRepository.test.ts

src/frontend/src/
├── domain/
│   └── entities/
│       └── RemovalRequestRecord.ts           ← new frontend entity
├── application/
│   └── useCases/
│       └── getRemovalRequests.ts             ← new use case
├── infrastructure/
│   └── api/
│       └── removalRequestsClient.ts          ← new API client
└── presentation/
    ├── components/
    │   └── RequestsTable/
    │       └── RequestsTable.tsx             ← new MUI table component
    ├── pages/
    │   └── RequestsDashboardPage.tsx         ← new dashboard page
    └── App.tsx                               ← updated: add React Router + /requests route

src/frontend/tests/
├── unit/
│   ├── components/
│   │   └── RequestsTable.test.tsx
│   ├── pages/
│   │   └── RequestsDashboardPage.test.tsx
│   └── useCases/
│       └── getRemovalRequests.test.ts

src/Infra/
├── dynamodb.tf                               ← new: removal-requests table
├── lambda.tf                                 ← updated: 2 new Lambda functions + IAM policy for DynamoDB
├── api_gateway.tf                            ← updated: 2 new routes + CORS update (add GET)
└── variables.tf                              ← updated: add DYNAMODB_TABLE_NAME var
```

## Complexity Tracking

No constitution violations requiring justification.

> **Note on agent prompt**: The existing ElevenLabs agent prompt does not include a step to collect the user's name. Before end-to-end testing, the agent must be updated to ask for the user's full name and map it to the `user_name` Data Collection variable. This is out of scope for the code tasks but must be done as a manual setup step.
